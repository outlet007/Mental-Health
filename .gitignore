node_modules/
public/css/output.css
.env
*.log
.DS_Store
Thumbs.db
.claude/

# Real client/counselor PII and health-related data — never commit (security
# review finding #1). content.json/schedules.json/survey-email-settings.json
# stay tracked since they're site config, not personal data.
data/admins.json
data/appointments.json
data/audit-log.json
data/clients.json
data/contacts.json
data/counselors.json
data/surveys.json
data/email-view-tokens.json

# Local backup snapshots (src/utils/backup.js) — same real PII/health data as
# the files above, just copies of it. Never commit.
backups/

# Error/info logs (src/utils/logger.js) — already caught by *.log above, but
# explicit for clarity since error entries can include client-identifying
# request context.
logs/
