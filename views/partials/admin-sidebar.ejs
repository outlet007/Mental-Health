<aside class="fixed inset-y-0 left-0 w-64 bg-white border-r border-slate-100 flex flex-col z-30">
  <!-- Logo -->
  <div class="px-6 py-5 border-b border-slate-100">
    <div class="flex items-center gap-3">
      <div class="w-9 h-9 rounded-xl bg-gradient-to-br from-brand-500 to-ocean-500 flex items-center justify-center">
        <i data-lucide="heart-handshake" class="w-5 h-5 text-white"></i>
      </div>
      <div>
        <p class="font-bold text-slate-800 leading-tight">MindWell</p>
        <p class="text-xs text-slate-400">ระบบจัดการ</p>
      </div>
    </div>
  </div>

  <!-- Nav -->
  <nav class="flex-1 px-3 py-4 space-y-0.5 overflow-y-auto">
    <p class="px-3 py-1.5 text-xs font-semibold text-slate-400 uppercase tracking-wider">ภาพรวม</p>
    <a href="/admin" class="nav-link <%= page==='dashboard' ? 'active' : '' %>">
      <i data-lucide="layout-dashboard" class="w-4 h-4"></i>
      แดชบอร์ด
    </a>

    <p class="px-3 pt-4 pb-1.5 text-xs font-semibold text-slate-400 uppercase tracking-wider">จัดการ</p>
    <a href="/admin/counselors" class="nav-link <%= page==='counselors' ? 'active' : '' %>">
      <i data-lucide="user-check" class="w-4 h-4"></i>
      นักให้คำปรึกษา
    </a>
    <a href="/admin/appointments" class="nav-link <%= page==='appointments' ? 'active' : '' %>">
      <i data-lucide="calendar-check" class="w-4 h-4"></i>
      นัดหมาย
    </a>
    <a href="/admin/clients" class="nav-link <%= page==='clients' ? 'active' : '' %>">
      <i data-lucide="users" class="w-4 h-4"></i>
      ผู้รับบริการ
    </a>
    <a href="/admin/schedules" class="nav-link <%= page==='schedules' ? 'active' : '' %>">
      <i data-lucide="clock" class="w-4 h-4"></i>
      ตารางเวลา
    </a>
  </nav>

  <!-- Footer -->
  <div class="px-4 py-4 border-t border-slate-100">
    <div class="flex items-center gap-3 px-2">
      <div class="w-8 h-8 rounded-full bg-brand-100 flex items-center justify-center text-brand-700 font-semibold text-sm">A</div>
      <div class="flex-1 min-w-0">
        <p class="text-sm font-medium text-slate-700 truncate">Admin</p>
        <p class="text-xs text-slate-400 truncate">admin@mindwell.th</p>
      </div>
    </div>
    <a href="/" target="_blank" class="mt-3 flex items-center gap-2 px-2 py-1.5 text-xs text-slate-500 hover:text-brand-600 transition-colors">
      <i data-lucide="external-link" class="w-3.5 h-3.5"></i>
      ดูหน้าเว็บหลัก
    </a>
  </div>
</aside>
